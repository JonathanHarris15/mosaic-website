# Operator (Cursor Cloud + Grok Bot)

**This skill is run by Cursor Cloud Agents.**

**Grok Bot is the project owner.** It answers routine questions for Jonathan (status, next ticket, whether a PRD is complete, whether Build may start) and escalates **only crucial decisions** — product locks, board-integrity calls, and anything that would invent behavior.

Do not wait on Jonathan for routine clarifications the ticket, `CONTEXT.md`, ADRs, or `CLAUDE.md` already settle. Do not invent product behavior to skip an escalation.
