**BackLink** — The way out of a page, top left. Eight pages wrote the same arrow-and-label by hand.

```html
<a class="m-back" href="index.html"><span class="material-symbols-outlined">arrow_back</span><span class="m-label">Home</span></a>
```

Base class `.m-back`, modifiers `.m-back--<variant>`.

Built from the Mosaic tokens only — no raw colours, no second icon set.
Icons are Material Symbols Outlined.
