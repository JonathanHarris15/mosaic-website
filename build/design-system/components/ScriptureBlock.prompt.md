**ScriptureBlock** — A quoted passage set in EB Garamond with a gold hairline down its edge, and the reference beneath in tracked caps.

```html
<blockquote class="m-scripture">For God so loved the world…<cite class="m-label">John 3:16</cite></blockquote>
```

**align:** `left` · `center`

Base class `.m-scripture`, modifiers `.m-scripture--<variant>`.

Built from the Mosaic tokens only — no raw colours, no second icon set.
Icons are Material Symbols Outlined.
